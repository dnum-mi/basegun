name: Push Docker image to GitHub container
on:
  push:
    tags:
      - 'v*'
jobs:
  release_github:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the repo
      uses: actions/checkout@v2
    - name: Publish release
      uses: release-drafter/release-drafter@v5
      with:
        name: ${{ github.ref }}
        tag: ${{ github.ref }}

  build_and_push:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the repo
      uses: actions/checkout@v2
    - name: Build the Docker image
      run: TAG=action docker-compose -f docker-compose-prod.yml build
    - name: Login to GHCR
      if: success()
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Push on GHCR
      run: |
        docker tag basegun-front:action-prod ghcr.io/datalab-mi/basegun/basegun-frontend:${{ github.ref }}
        docker tag basegun-back:action-prod ghcr.io/datalab-mi/basegun/basegun-backend:${{ github.ref }}
        docker push ghcr.io/datalab-mi/basegun/basegun-frontend:${{ github.ref }}
        docker push ghcr.io/datalab-mi/basegun/basegun-backend:${{ github.ref }}
        docker tag ghcr.io/datalab-mi/basegun/basegun-frontend:${{ github.ref }} ghcr.io/datalab-mi/basegun/basegun-frontend:latest
        docker tag ghcr.io/datalab-mi/basegun/basegun-backend:${{ github.ref }} ghcr.io/datalab-mi/basegun/basegun-backend:latest
        docker push ghcr.io/datalab-mi/basegun/basegun-frontend:latest
        docker push ghcr.io/datalab-mi/basegun/basegun-backend:latest